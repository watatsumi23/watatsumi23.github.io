---
import BaseLayout from '../layouts/BaseLayout.astro'
import { SITE_BASE, SocialLinks } from '../consts'
import { Icon } from 'astro-icon/components'
import BlogCard from '../components/BlogCard.astro'
import { getCollection } from 'astro:content'

const posts = (await getCollection('blog'))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 4)
---

<BaseLayout>
	<main
		class="app-container flex flex-col gap-12 justify-center py-36 md:py-48 pt-44 md:pt-56 relative"
	>
		<div>
			<p class="block text-3xl opacity-70 mt-9 mb-4">Welcome to</p>
			<h1 class="text-8xl font-bold leading-none">Archives of the Gap</h1>
		</div>
		<p class="text-2xl max-w-2xl font-light leading-normal">
			This blog is my digital laboratory. It's the logbook where I document my practices, experiments, and findings in the fascinating world of penetration testing.
		</p>
		<div class="flex flex-wrap gap-8">
			{
				SocialLinks.map((link) => (
					<a
						class="text-xl text-primary dark:text-primary-dark underline hover:underline underline-offset-4"
						href={link.url}
						target="_blank"
					>
						{link.name}
					</a>
				))
			}
		</div>
	</main>

	<!-- Recent Posts Section -->
	<section class="app-container">
		<h2 class="section-title">Recent Posts</h2>
		<div class="posts-grid-container">
			{posts.map((post) => <BlogCard post={post} />)}
		</div>
		<a class="btn-secondary" href="/blog"
			>Read All
			<Icon size={20} name="mdi:arrow-right" />
		</a>
	</section>

	<!-- Recent Posts Section -->
	<section class="app-container py-42 grid lg:grid-cols-2 gap-28 lg:gap-10">
		<div class="space-y-6">
			<h2 class="section-title">About Me</h2>
			<p class="text-lg max-w-xl pb-4">
				Hi, I'm Felipe, a passionate Information Technology Security student. My journey in this field began with a deep curiosity to understand how systems work, not just superficially, but by delving into their deepest foundations.

Currently, I'm immersed in the fascinating world of Pentesting (Penetration Testing). For me, pentesting isn't just a technical discipline; it's a mindset. It's the ability to "think like the adversary" to identify and understand vulnerabilities before they can be exploited by malicious actors.
			</p>
			<a class="btn-secondary" href={`watatsumi23.github.io/pages/about.astro`}
				>Go to About
				<Icon size={20} name="mdi:arrow-right" />
			</a>
		</div>
		<div class="space-y-6">
			<h2 class="section-title">Certifications</h2>
			<p class="text-lg max-w-xl">
				1- Google Cybersecurity
			</p>
			<div class="flex flex-wrap mt-auto space-x-4">

			</div>
		</div>
	</section>
</BaseLayout>
